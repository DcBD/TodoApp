<UserControl x:Class="DesktopWPFUI.Controls.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopWPFUI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />

        </Grid.ColumnDefinitions>

        <ComboBox Name="FilterComboBox" Grid.Column="1" SelectedIndex="0">
            <ComboBoxItem Name="FilterAll" Content="All" />
            <ComboBoxItem Name="FilterInProgress" Content="In progress" />
            <ComboBoxItem Name="FilterFinished" Content="Finished" />
        </ComboBox>

        <Button Grid.Column="2" Name="CreateTaskButton" Content="Add" Padding="5" Click="CreateTaskButton_Click"/>
    </Grid>
</UserControl>
